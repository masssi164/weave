TF_VAR_docker_network_name=weave_network

TF_VAR_tenant_domain=weave.local
# Enable only for local integration testing.
# TF_VAR_create_test_user=true
TF_VAR_auth_subdomain=auth
TF_VAR_api_subdomain=api
TF_VAR_nextcloud_subdomain=files
TF_VAR_matrix_subdomain=matrix

TF_VAR_public_scheme=https

# Canonical single-stack ports.
# Use these only on a machine where Weave owns the standard local ports.
# TF_VAR_proxy_http_host_port=80
# TF_VAR_proxy_host_port=443
# TF_VAR_keycloak_host_port=8080
# TF_VAR_keycloak_management_host_port=9000
# TF_VAR_mas_host_port=8082
# TF_VAR_synapse_host_port=8008
# TF_VAR_nextcloud_host_port=8083
# TF_VAR_backend_host_port=8084

# Required shared-host isolation block.
# install.sh defaults to this block so Weave does not collide with another local stack.
TF_VAR_proxy_http_host_port=44080
TF_VAR_proxy_host_port=44443
TF_VAR_keycloak_host_port=48080
TF_VAR_keycloak_management_host_port=49000
TF_VAR_mas_host_port=48082
TF_VAR_synapse_host_port=48008
TF_VAR_nextcloud_host_port=48083
TF_VAR_backend_host_port=48084

TF_VAR_nextcloud_trusted_proxies=172.16.0.0/12
TF_VAR_api_upstream=weave-backend:8080

TF_VAR_proxy_image=caddy:2.8

# Provider-stack posture: default exposes backend-owned provider seams but keeps every
# optional/heavy runtime fail-closed unless explicitly profiled and secret-backed.
TF_VAR_provider_stack_profile=fail-closed
TF_VAR_provider_stack_readiness=fail-closed

# DevOps provider candidates: GitLab CE/FOSS is primary; Forgejo is a first-class
# alternative. Both are disabled by default, read-only until reviewed, and tokens
# belong only in private env files.
TF_VAR_devops_primary_provider=gitlab-ce-foss
TF_VAR_devops_alternative_provider=forgejo
TF_VAR_devops_gitlab_runtime_enabled=false
TF_VAR_devops_gitlab_base_url=
TF_VAR_devops_gitlab_writes_enabled=false
TF_VAR_devops_forgejo_runtime_enabled=false
TF_VAR_devops_forgejo_base_url=
TF_VAR_devops_forgejo_writes_enabled=false
# Set TF_VAR_devops_gitlab_api_token / TF_VAR_devops_forgejo_api_token in a private env file only.
# install.sh does not persist provider API tokens into .generated/bootstrap.env;
# source the same private env file when running live provider profiles.

# Office provider candidates: ONLYOFFICE Docs Community is the default candidate
# through the Nextcloud integration path behind the backend facade. Collabora/CODE
# stays non-default because licensing/runtime fit still needs proof.
TF_VAR_office_primary_provider=onlyoffice-community
TF_VAR_office_onlyoffice_runtime_enabled=false
TF_VAR_office_onlyoffice_document_server_url=
TF_VAR_office_nextcloud_integration_mode=nextcloud-onlyoffice-app-behind-backend-facade
TF_VAR_office_collabora_runtime_enabled=false
# Set TF_VAR_office_onlyoffice_jwt_secret in a private env file only.
# If enabling the onlyoffice compose profile, also set
# TF_VAR_office_onlyoffice_jwt_enabled=true with a non-empty JWT secret.

# Nextcloud groupware provider seams from backend PR #104 stay visible but disabled
# until the backend contracts are merged and live-validated.
TF_VAR_groupware_contacts_runtime_enabled=false
TF_VAR_groupware_forms_runtime_enabled=false

# Leave these unset for install.sh defaults. If you override them, prefer paths
# inside this workspace's generated cert directory or operator-managed TLS paths.
# TF_VAR_caddy_tls_cert_file=./01-infrastructure/.generated/caddy/certs/weave.local.pem
# TF_VAR_caddy_tls_key_file=./01-infrastructure/.generated/caddy/certs/weave.local-key.pem
# TF_VAR_caddy_tls_ca_file=./01-infrastructure/.generated/caddy/certs/weave-local-ca.pem

# Optional runner-hygiene toggles for self-hosted CI or any shared Docker host.
# WEAVE_RUNNER_HYGIENE=true
# WEAVE_REMOVE_VOLUMES=true

WEAVE_CADDY_CONTAINER_NAME=weave-proxy
WEAVE_CADDYFILE_PATH=./01-infrastructure/.generated/caddy/Caddyfile
WEAVE_CADDY_CERTS_DIR=./01-infrastructure/.generated/caddy/certs

# Optional Boards/OpenProject read-sync gates. Defaults keep the provider path disabled
# and the core stack independent from OpenProject.
TF_VAR_boards_preview_runtime_enabled=false
TF_VAR_boards_preview_provider=local-preview
TF_VAR_boards_openproject_runtime_enabled=false
TF_VAR_boards_openproject_read_sync_enabled=false
TF_VAR_boards_openproject_context_authorization_enabled=false
TF_VAR_boards_openproject_audit_consent_enabled=false
TF_VAR_boards_openproject_provider_writes_enabled=false
TF_VAR_boards_nextcloud_deck_runtime_enabled=false
TF_VAR_boards_openproject_auth_mode=disabled
TF_VAR_boards_openproject_base_url=
# When enabling read-only OpenProject sync, set provider/runtime/read-sync/context-authorization true,
# auth-mode=service-token, base-url, and keep provider writes false.
# Keep token unset/blank unless explicitly validating read-only OpenProject sync.
# Set TF_VAR_boards_openproject_api_token in a private env file only

# Optional self-hosted OpenProject profile (docker compose -f docker-compose.openproject.yml --profile openproject up -d).
TF_VAR_openproject_image=openproject/openproject:15
TF_VAR_openproject_host_port=48086
# Set TF_VAR_openproject_secret_key_base in a private env file only
