tasks.register('canonicalDataCi') {
    group = 'verification'
    description = 'Run provider-independent canonical identity, transfer-envelope, loss-accounting, checkpoint, and connector-coordination tests.'
    dependsOn ':weave-application-core:test'
}
