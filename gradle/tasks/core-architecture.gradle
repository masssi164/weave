tasks.register('coreArchitectureCi') {
    group = 'verification'
    description = 'Enforce canonical dependency boundaries and contract-only architecture proofs.'

    dependsOn ':weave-application-core:test',
            ':weave-files-core:test',
            ':weave-mcp-server:test',
            ':weave-product-e2e:enterpriseBoundaryPoc'
}
