# Superseded provider-boundary document

This path is retained so older links remain understandable. It is not current architecture authority.

Use [Weave data-sovereignty core](data-sovereignty-core.md) and [Canonical transfer kernel](canonical-transfer-kernel.md).

The current rule is: canonical application services are authoritative; JPA/BlobStore are persistence or infrastructure adapters; external systems are source/target provider connectors; `weave-native` is composition, not a second provider-owned business implementation.
